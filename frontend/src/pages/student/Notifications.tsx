import NotificationCenter from '../../components/NotificationCenter';

export default function StudentNotifications() {
  return (
    <NotificationCenter
      title="Central de comunicados"
      subtitle="Todas as notificacoes da equipe MAEXTRIA para voce."
      role="student"
    />
  );
}
